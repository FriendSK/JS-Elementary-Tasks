var inputValue = prompt("Enter a  min length");
var inputValue = prompt("Enter a  max length");
var value1 = parseFloat(inputValue).toFixed(0);
var value2 = parseFloat(inputValue).toFixed(0);

var context = {
    min: value1,
    max: value2
};